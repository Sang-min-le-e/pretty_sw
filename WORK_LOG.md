# 작업 기록 (자동 진행 로그)

사용자가 자리를 비운 동안 질문 없이 진행하며 남기는 기록입니다. 시간순으로 아래에 계속 추가합니다.

## 2026-09-04

### 이전까지 완료한 것 (자리 비우기 전)
- Flutter SDK 설치 (`~/development/flutter`, stable 3.47.2), PATH 등록 (`~/.zshrc`)
- Flutter 프로젝트 스캐폴딩 (`routine_app`, org `com.prettysw`, 플랫폼 android/ios/web)
- 의존성 추가: `flutter_riverpod`, `go_router`, `flutter_reactive_ble`, `hive`/`hive_flutter`, `flutter_tts`, `flutter_local_notifications`, `dio`
- feature-based 폴더 구조 생성 (`lib/app`, `lib/core/{ble,storage,network}`, `lib/features/{routine,watch_connection}/{data,domain,presentation}`)
- 기본 화면 2개(루틴 목록, 워치 연결) + 라우터 + 접근성 테마(큰 글씨/버튼) 뼈대 작성
- `flutter analyze` 통과, `flutter build web` 성공 확인
- git 저장소 초기화, GitHub 공개 저장소 생성/push 완료 → https://github.com/Sang-min-le-e/pretty_sw

### 지금부터 (자리 비운 동안) 하는 일
1. Figma 디자인(`예소` 파일, node-id=0-1)에서 "앱 초안" 프레임들을 전수 조사해서 실제 화면 구성/문구/플로우를 파악
2. 파악한 화면 구조를 Flutter 코드에 100% 반영 (화면, 위젯, 라우팅 흐름)
3. 학습 목적이라고 하셔서 코드 전반에 "왜 이렇게 했는지" 설명하는 주석을 평소보다 훨씬 많이 추가
4. 진행 상황을 이 파일에 계속 기록

---

### Figma 조사 결과 (node-id=0-1, "예소" 파일, 보기 전용 접근)

파일 자체는 로우파이 브레인스토밍 보드라 화면 목업은 대부분 아직 러프한 스케치 수준이었지만,
"앱 초안" 프레임 옆에 붙어있는 텍스트 노트(이미지로 붙여넣어진 것들, `image 22`~`image 25`)에
실제 UX 시나리오가 번호별로 정리되어 있었음. 이게 이번 구현의 기준 스펙.

**컨셉 원본 메모 (Slide 16:9 - 1):**
> ADHD나 발달장애를 겪는 사람들을 위한 키링형, 혹은 밴딩형 디바이스.
> 눈에 보이지 않는 시간을 계획하거나, 루틴을 기억하고 실행하는데 어려움을 겪음.
> 독립형 디바이스 (걸리적 거리는 짐 X -> 친근한 내 편(보조자)).
> 루틴, 알람 & 체크: 부드러운 진동, 화면에 도트 그래픽으로 미션이 뜸 -> 미션 성공 -> 화면 속 캐릭터 성장 or 무작위 선물 뽑아줌.
> 보호자가 루틴을 짜줌.

즉 **보호자(guardian)가 이 앱으로 자녀/피보호자의 루틴을 등록**하고, 워치가 그 루틴을 미션으로
보여주면서 완료 시 캐릭터가 성장하거나 랜덤 선물을 주는 게이미피케이션 구조.

**1. 초기 설정 (`image 22`)**
- 소셜 로그인 (구글, 카카오 등)
- 1차: 보호자 성명, 관계
- 2차: 자녀 성명, 자녀 생년월일
- 3차: 페어링 + 디바이스 서버 등록
- 페어링까지 완료 후 "메인창"(4번)으로 이동

**2. 루틴 등록 (`image 23`) — 가장 중요한 도메인 모델**
- **빅루틴(BigRoutine)**: 시간 정하기 가능 (ex: 8:00~12:00)
  - 설정값1: 고정 빅루틴 (앞으로 등록 시 디폴트로 생성, 내부 스몰루틴도 자동 디폴트)
  - 설정값2: 날짜 선택 / 연속 날짜 선택 (ex: 16일~20일), 내부 스몰루틴도 자동 디폴트
  - 수정(시간, 제목) / 삭제 가능
- **스몰루틴(SmallRoutine)**: 빅루틴 내부의 할 일. **개별 시간 없음**, 순서가 있는 연속적 할 일
  - 예: 1.씻기 2.문잠구기 3.301번버스타기 4.약국도착하기 5.집복귀
  - 수정/삭제 가능
- **루틴등록 UX 플로우**: 메인창의 캘린더 -> 날짜 클릭 -> "루틴 등록" 창 -> 빅루틴 생성 -> 빅루틴 클릭 시 팝업으로 스몰루틴 작성

**3.** — 해당 노트(`image 24`, 2개)는 실제로는 빈 이미지였음 (placeholder만 있고 내용 없음). 스킵.

**4. 내 기기 (디바이스 정보 파악) - 메인 창 (`image 25`)**
- 자녀의 미션 이행률/기기 배터리 등을 보는 검사겸사 메인창
  1. 이번주 미션 이행률
  2. 어제 미션 이행률
  3. 마지막 동기화 시점 / 배터리 잔량 (충전 필요 여부 확인 목적)
- 예시 알림 문구: `"OO" 미션에 대해 이행률이 낮아요`, `이번 n일 동안의 이행률이 평균보다 낮아졌어요`
- (미정) 자녀가 집(와이파이 연결)에 있을 때 한정 실시간 대시보드 아이디어 — 아직 미정이라 이번 구현에는 반영 안 함

**캐릭터/먹이**: 별도 "캐릭터, 먹이 레퍼런스" 프레임은 무드보드(핀터레스트 느낌 참고 이미지 모음)라
구체 스펙은 없음 — 다마고치류 컴패니언 캐릭터에게 "먹이"를 주는 방식의 보상 연출이라는 컨셉만 확인.

**범위 밖으로 판단해 이번엔 구현 안 한 것**: 댓글에 언급된 "커뮤니티(스도방/중고거래방/소통방)" 아이디어는
디자이너 코멘트일 뿐 "앱 초안" 스펙에 없어서 제외.

---

### 구현 내용 (Figma 스펙을 코드로 옮긴 것)

**도메인 모델 (전면 재작성)**
- `lib/features/routine/domain/big_routine.dart`, `small_routine.dart` — 빅루틴/스몰루틴 구조.
  기존에 있던 단순 `Routine{steps: List<String>}` 모델은 지우고 이걸로 교체.
- `lib/features/onboarding/domain/guardian_profile.dart`, `child_profile.dart`
- `lib/features/character/domain/companion_character.dart` — 캐릭터/먹이 보상 구조

**화면 흐름 (go_router, `lib/app/router.dart`)**
```
/onboarding/social-login → /onboarding/guardian-info → /onboarding/child-info
  → /onboarding/device-pairing → (완료) → /  (대시보드)
/  (대시보드, "4. 내 기기" 메인창)
  → /calendar (달력)
    → /calendar/:date/routines (그 날짜의 빅루틴 목록 + 빅루틴 생성)
      → (빅루틴 탭) 스몰루틴 편집 바텀시트
/watch-connection (대시보드에서 언제든 재진입 가능한 워치 재연결 화면)
```
온보딩 완료 여부는 Hive에 저장해두고, `main.dart`에서 앱 시작 전에 먼저 확인해서
라우터의 시작 위치(`initialLocation`)를 정한다 (완료했으면 로그인 화면을 다시 안 보여줌).

**저장소**: Hive 박스 3개 — `big_routines`(빅루틴, 스몰루틴은 그 안에 내포), `onboarding`(보호자/자녀 정보 + 페어링 완료 여부), `character`(먹이 준 횟수).

**패키지 추가**: `table_calendar`(달력 UI), `uuid`(id 생성), `intl`+`flutter_localizations`(한국어 날짜/위젯 로케일).

**주석 스타일**: 사용자가 "코드를 직접 이해하고 유지보수하고 싶다"고 해서, 이번 구현은
평소보다 훨씬 자세하게 "왜 이렇게 짰는지"를 각 파일 상단/주요 지점에 한국어로 적어뒀다.
(Riverpod의 Provider/FutureProvider 구분, Hive 직렬화, go_router 온보딩 리다이렉트 전략 등)

**실행 검증**: `flutter build web`로 빌드해서 로컬 정적 서버(8765 포트)로 띄운 뒤, Chrome
자동화로 온보딩 전체 흐름(로그인→보호자 정보→자녀 정보 생년월일 선택기→페어링)부터
캘린더 → 날짜 클릭 → 빅루틴 생성 → 스몰루틴 추가 → 완료 체크 → 대시보드에 이행률
100%/캐릭터 먹이 1회 반영까지 실제로 클릭해보며 확인함. 그 과정에서 찾은 버그 1개
(테이블 캘린더 요일 줄이 접근성 큰글씨 테마 때문에 세로로 잘려 보이던 문제)를
`daysOfWeekHeight` 조정으로 수정함. `flutter analyze`도 이슈 0개.

**중요 — 웹은 검증용일 뿐, 실제 타겟은 모바일**: `flutter_reactive_ble`(BLE 스캔)는 웹을
지원하지 않아서(`dart:io Platform` 미지원) 웹 빌드에서 워치 스캔 화면은 항상 에러가 뜬다.
이건 버그가 아니라 웹이 원래 이 앱의 타겟 플랫폼이 아니기 때문 — 실제 BLE 연동은
Android/iOS 실기기에서 확인해야 한다. 그래서 온보딩 페어링 화면에 "워치 없이
건너뛰기(임시)" 버튼을 넣어뒀다.

**아직 안 한 것 / 다음에 할 일**
- 백엔드 API 연동 (지금은 전부 로컬 Hive 저장) — `core/network/api_client.dart`에 Dio는
  세팅해뒀지만 실제 엔드포인트 호출은 백엔드 팀과 스펙 맞춘 뒤에 붙이면 됨.
- 소셜 로그인(구글/카카오) 실제 OAuth 연동.
- 워치 GATT 스펙 확정 후 `core/ble/ble_service.dart`에 실제 캐릭터리스틱 읽기/쓰기 추가,
  대시보드의 "마지막 동기화/배터리" 카드를 실제 값으로 교체.
- 캐릭터 실제 일러스트/애니메이션 (지금은 이모지로 임시 표현).
- Android SDK가 이 개발 환경에 아직 없어서 실기기/에뮬레이터 실행은 못 해봄 — 필요하면
  Android Studio 설치부터 다시 요청하면 됨.

**GitHub**: 전부 커밋해서 https://github.com/Sang-min-le-e/pretty_sw (main 브랜치)에 push 완료.

---
